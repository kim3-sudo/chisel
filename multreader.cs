using System;
using System.Collections.Generic;
using System.Text;

namespace Chisel
{
    //purpose: read in a plaintext file line-by-line and put the address in each line into a string object
    class multreader
    {
        public string reader(string multFileName)
        {
            
            return "failure";
        }
    }
}
